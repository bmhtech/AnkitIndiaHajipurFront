import { StockTransferChallan } from './stock-transfer-challan';

describe('StockTransferChallan', () => {
  it('should create an instance', () => {
    expect(new StockTransferChallan()).toBeTruthy();
  });
});
