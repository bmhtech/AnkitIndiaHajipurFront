import { StockTransferPurchaseInvoice } from './stock-transfer-purchase-invoice';

describe('StockTransferPurchaseInvoice', () => {
  it('should create an instance', () => {
    expect(new StockTransferPurchaseInvoice()).toBeTruthy();
  });
});
