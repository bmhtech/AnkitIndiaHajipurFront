import { ProdOutputDetails } from './prod-output-details';

describe('ProdOutputDetails', () => {
  it('should create an instance', () => {
    expect(new ProdOutputDetails()).toBeTruthy();
  });
});
