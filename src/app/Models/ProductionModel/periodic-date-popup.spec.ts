import { PeriodicDatePopup } from './periodic-date-popup';

describe('PeriodicDatePopup', () => {
  it('should create an instance', () => {
    expect(new PeriodicDatePopup()).toBeTruthy();
  });
});
