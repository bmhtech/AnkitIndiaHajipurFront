import { ProductionTransactionSpecial } from './production-transaction-special';

describe('ProductionTransactionSpecial', () => {
  it('should create an instance', () => {
    expect(new ProductionTransactionSpecial()).toBeTruthy();
  });
});
