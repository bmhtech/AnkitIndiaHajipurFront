import { ProductionTransaction } from './production-transaction';

describe('ProductionTransaction', () => {
  it('should create an instance', () => {
    expect(new ProductionTransaction()).toBeTruthy();
  });
});
