import { ReturnApprovalNote } from './return-approval-note';

describe('ReturnApprovalNote', () => {
  it('should create an instance', () => {
    expect(new ReturnApprovalNote()).toBeTruthy();
  });
});
