import { PartyBillPaymentTo } from './party-bill-payment-to';

describe('PartyBillPaymentTo', () => {
  it('should create an instance', () => {
    expect(new PartyBillPaymentTo()).toBeTruthy();
  });
});
