import { PurReturnApprovalNote } from './pur-return-approval-note';

describe('PurReturnApprovalNote', () => {
  it('should create an instance', () => {
    expect(new PurReturnApprovalNote()).toBeTruthy();
  });
});
